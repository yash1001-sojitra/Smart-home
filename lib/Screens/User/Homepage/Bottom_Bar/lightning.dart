import 'package:flutter/material.dart';

class LightningDash extends StatelessWidget {
  const LightningDash({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}